import { Graph } from "../../../../types";
export declare const canUpdateOrgSettings: (graph: Graph.Graph, currentUserId: string) => boolean, canRenameOrg: (graph: Graph.Graph, currentUserId: string) => boolean, canDeleteOrg: (graph: Graph.Graph, currentUserId: string) => boolean;
//# sourceMappingURL=orgs.d.ts.map