import { Graph } from "../../../../types";
export declare const canManageUserGroups: (graph: Graph.Graph, currentUserId: string) => boolean;
export declare const canManageAppGroups: (graph: Graph.Graph, currentUserId: string) => boolean;
export declare const canManageBlockGroups: (graph: Graph.Graph, currentUserId: string) => boolean;
//# sourceMappingURL=groups.d.ts.map