{
  "name": "@envkey/infra",
  "version": "2.0.0",
  "description": "EnvKey infrastructure shared libraries",
  "author": "Envkey Inc",
  "license": "MIT",
  "private": true,
  "main": "src/lib.ts",
  "scripts": {
    "build": "echo 'Infra package uses ts-node at runtime - no build needed'",
    "watch": "tsc --watch --noEmit -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "check-watch": "tsc --watch --noEmit -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/node": "^18.18.0",
    "@types/node-fetch": "^2.5.7",
    "@types/ramda": "^0.27.32",
    "@types/semver": "^7.3.4",
    "rimraf": "^5.0.5",
    "typescript": "~5.3.3"
  },
  "dependencies": {
    "@envkey/core": "workspace:*",
    "aws-sdk": "^2.934.0",
    "fast-xml-parser": "^4.0.0",
    "node-fetch": "^2.6.1",
    "ramda": "^0.27.1",
    "semver": "^7.3.2"
  }
}
